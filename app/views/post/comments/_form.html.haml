= simple_form_for @post.comments.build, url: post_comments_path(@post), :html => { :class => 'form-horizontal' } do |f|
  = f.input :commenter
  = f.input :body, :input_html => { :rows => 5 }
  = f.button :submit, 'Save', :class => 'btn btn-success'